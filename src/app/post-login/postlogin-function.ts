// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for PostloginFunction
 */
export interface PostloginFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/post-login/postlogin.
 */
export class PostloginFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: PostloginFunctionProps) {
    super(scope, id, {
      description: 'src/app/post-login/postlogin.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/post-login/postlogin.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}