// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for SportoverviewFunction
 */
export interface SportoverviewFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/sportoverview/sportoverview.
 */
export class SportoverviewFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: SportoverviewFunctionProps) {
    super(scope, id, {
      description: 'src/app/sportoverview/sportoverview.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs20.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/sportoverview/sportoverview.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}